<div class="container">
   <div class="row">
       <div class="col">
        {{#each model as |recipe|}}
            {{#link-to 'recipes.show' recipe.id}}
                {{recipe-single recipe.imageURL recipe.name}}
            {{/link-to}}
         {{/each}}
       </div>
       <div class="col">
           {{recipe-add}}
       </div>
    </div>
</div>
{{outlet}}