<div class="container">
   {{#each model as |recipe|}}
        {{#link-to 'recipes.show' recipe.id}}
            {{recipe.name}}
            <img src={{recipe.imageURL}} />
        {{/link-to}}
    {{/each}}
</div>
{{outlet}}